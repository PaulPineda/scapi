const assert = require('assert');
const dummyJSON = require('../../app/dataAPI/_dummy_0JSONSerialisedContent');
describe('Create flatArray from content JSON', function(){
  describe('filter COLUMNS to only useful ones', function(){
    it('has a function')
  });
});



// reduces the COLUMN and DATA arrays into an array of content elements
// - each element will have a 'parent' attribute added to it according to the
//   chronological order it appears in the array
//
// RETURNS
